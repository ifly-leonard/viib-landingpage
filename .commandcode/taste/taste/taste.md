# Taste
- Prefers reusable components over duplicating markup across pages; when content appears in multiple places, wants it extracted into shared components and a single source-of-truth data module so the places stay in sync. Confidence: 0.9
- For UI-guide/design-reference pages, wants them to render the literal production components (direct imports, same props and markup) rather than recreated approximations, and wants every block modular/independently importable so the guide is "high fidelity" with the real pages — pure composition, no copied or simplified mockups. Confidence: 0.8
- Prefers following shadcn/ui conventions for UI primitives (e.g., building a page header's breadcrumbs from shadcn's `breadcrumb.tsx` primitives rather than hand-rolling). Confidence: 0.9
- Prefers content to be JSON-driven rather than inline TS data: one JSON file per content item with frontmatter-style metadata (title, label, description, colors, variant) plus a `content` body, an `index.json` for ordering, and a typed loader that merges them into a typed array. Confidence: 0.9
- New pages and components must match the site's existing theme — reuse the established design tokens (CSS variables like `--vil-*`/`--text-*`, utility classes like `viiv-kicker`, `viiv-section-title`, display font) rather than introducing a divergent look; explicitly dislikes headers with off-theme design, fonts, or colors (e.g., a dark navy hero with italic headline on a light ivory themed site). This extends to derived/social assets: the OG/meta image must be on-theme too — light (ivory/white) background matching the site, the real brand logo, the brand display font, and navy/gold accents — and an off-theme card (wrong background, no logo, generic font) is called out as "not even remotely close" and must be redesigned. Re-confirmed when commissioning a scroll-animated mentors section: the card's "design, border and color" had to match the site. Confidence: 0.95
- When the user says a new section should "match" an existing section's UI (e.g., "should match the founder section (on home page) in terms of UI"), expects the agent to study that section's actual markup and replicate its exact visual pattern — layout grid, photo treatment (circular portrait with white border), name/role/credential arrangement, kicker, and copy typography — rather than inventing a new design interpretation. A bespoke testimonial-style card (rounded-rectangle portrait with gradient overlay, gold-bordered pull-quote callout, icon badge chips) for the same content was explicitly rejected as "horrible" and had to be rewritten to mirror the founder section's structure. Confidence: 0.9
- Likes the hero section's visual treatment enough to want it reused on interior page headers: blue/navy background with a cover image behind it plus gradient scrims for readability, and light ivory/gold text — even when the rest of the page is light-themed. Confidence: 0.8
- Explicitly likes the site's display font for headlines and wants it kept even when other header attributes (background, colors) change. Confidence: 0.8
- Prefers configuration to be file-based in a TypeScript config file (e.g., `config.ts`) rather than environment variables — explicitly asked for "file based please, no .env" when making an external link (cal.com) configurable. Confidence: 0.9
- Expects data-file fields (e.g., `variant` in a book's JSON) to genuinely control component rendering; when a data field appears to have no visible effect, treats it as a bug to fix rather than accepting a silent default. Confidence: 0.7
- Wants per-item feature behavior (e.g., the flipbook lead-gate blur) to be opt-in and configurable per content item through the JSON data files — only the items the user explicitly configures get the behavior (e.g., a `gate` field only on student-handbook.json), never blanket-applied to all items. Confidence: 0.8
- Prefers long-form reading content to live on a single routed page per book with all chapters rendered inline (TOC uses in-page anchors) — explicitly removed separate chapter pages and the per-chapter route in favor of everything living on `library/[slug]` itself. This supersedes an earlier preference for one page per chapter. Confidence: 0.9
- Wants book/reading pages to be full-screen immersive sections that lead with Title, Author, a Table of contents, and the opening chapter's content. Confidence: 0.7
- For library/book reading pages, explicitly wants the entire book presented like an actual printed book — white/paper background (not a webpage look, e.g., the dark navy hero used on other pages), with book-like layout elements such as a title page, a contents page, serif reading typography, and page-style chapter navigation. This is a deliberate visual direction for reading content that overrides the usual site hero treatment. Confidence: 0.9
- Prefers cache-busting version query parameters (e.g., `?v=YYYYMMDD`) appended to static asset URLs (favicons, web manifest, etc.) so updates propagate without stale-cache issues — supplied these on every asset link rather than relying on default caching. Confidence: 0.8
- When the user pastes exact HTML/markup or code to implement (e.g., favicon link tags with specific paths, sizes, types, and version params; a package usage snippet with specific required props), expects the shipped output to follow that exact API/attributes verbatim rather than a close approximation. Confidence: 0.8
- The user verifies the actual served/rendered output in the browser, not just that the code or build looks right; when a requested change appears done but the site still shows framework-default assets (e.g., the Vercel default `src/app/favicon.ico` overriding custom favicon links), they report it and expect the root cause to be found and confirmed against the real served HTML — a passing build is not sufficient proof. When a rendered UI change looks wrong, they send a screenshot ("See for yourself") and expect the agent to actually inspect the image to see the real state rather than reason from code alone; when a claimed fix still "doesn't work" they repeat that the layout must be checked against the real rendered page — code-level verification (typecheck/lint passing, reading library internals) is never treated as proof the UI is right. Re-confirmed when a newly-added nav link drew "I can't find it on the navbar": the correct diagnosis came from fetching the live served HTML (the link WAS there) and explaining cache/production vs. local-dev causes plus where in the UI the item actually lives (inside the Campus Life dropdown/accordion, not the top-level bar) — the item's presence in the code/config is never enough. Confidence: 0.92

- Never fabricates content or data for a page: only real project data may be shown (addresses, phone numbers, contact details, statistics, testimonials); explicitly forbids inventing missing campus/contact info and forbids adding stock imagery when the project has no suitable existing asset. Confidence: 0.9
- When adding copy for a topic that already appears elsewhere on the site (e.g., a small brief about the partner university in a bento grid cell), prefers reusing the existing copy already on the site over drafting new text — explicitly said "whatever copy about that University that's already there on the site, reuse that." Confidence: 0.75
- Explicitly allows clearly-sanctioned dummy/placeholder data when the real values don't exist yet (e.g., "As of now, add dummy details" for payment/IFSC/UPI/DD/cheque information; "just put dummy images. I'll update real photos soon" for a new image gallery), treating it as temporary scaffolding to be swapped for real values later — a carve-out from the never-fabricate rule, which still applies to real-world facts unless the user explicitly opts them into placeholders. Re-confirmed when a new Accommodations page was explicitly allowed to ship with dummy images until real photos arrive. Confidence: 0.7

- When asked to update an existing page, prefers extending it while preserving its existing functionality and integrations (e.g., keep the Cal.com embed and its booking URL/config untouched, keep header/footer/navigation unchanged) rather than rebuilding the page from scratch. Re-confirmed when the Zoho CRM task explicitly required "Do not redesign or replace the existing form" — the form stays as-is and only gains the new submission wiring. Confidence: 0.9

- Explicitly requires interactive controls (buttons, accordion triggers) to be keyboard accessible and layouts to be fully responsive — multi-column on desktop, single-column on mobile. Confidence: 0.7

- Prefers third-party booking/calendar embeds (e.g., the Cal.com widget) to open in a modal dialog triggered by a CTA button rather than being embedded inline in the page flow. Confidence: 0.7

- Requires modals/dialogs to ALWAYS fit within the viewport — no overflow on shorter screens. Expects the dialog height capped to the viewport (e.g., `max-h-[calc(100dvh-2rem)]`) with a flexible/shrinking embed area (`h-[min(640px,calc(100dvh-11rem))]`) and internal scrolling, instead of a large fixed-height embed that pushes the modal off-screen. Re-confirmed when the user reported the global lead modal was "not fitting the screen, it's too big" and expected the size tightened (smaller max-width, viewport-capped height with internal scroll, no fixed `min-h` forcing height). Confidence: 0.9

- Prefers leveraging established third-party npm packages for complex features rather than building custom implementations — sourced `react-pdf-flipbook-viewer` themselves and asked to try it for the library books; re-confirmed when they explicitly directed "Use this package" for a WhatsApp floating button, supplying the exact repo URL (`lovely-whatsapp-fab`) and expecting the agent to research and integrate it as-is. Confidence: 0.75
- When a third-party library repeatedly fails to deliver the required behavior (e.g., react-flipbook never producing a two-page spread), prefers abandoning it and swapping to an alternative library rather than continuing to patch around the broken one — and will supply the specific replacement to try (e.g., "Instead of using react-flipbook, try https://github.com/taufiqelrahman/flipbook-js"), expecting the agent to research and integrate that library, even a vanilla-JS one wrapped for React. Confidence: 0.7
- When a third-party component behaves incorrectly (e.g., transparent pages in the flipbook), expects the agent to consult the official package documentation (user supplies the docs URL) to learn the correct usage/styling requirements before debugging — rather than guessing at causes or patching blind. Confidence: 0.8
- When a user-supplied npm package is fundamentally incompatible with the project (e.g., `lovely-whatsapp-fab` ships uncompiled JSX in a `.js` file that crashes Next.js/Turbopack SSR with `SyntaxError: Unexpected token '<'` and 500s the whole site), prefers the agent to drop the broken dependency and build a small self-contained component instead (same behavior: WhatsApp green button bottom-right, idle float animation, `wa.me` link with prefilled message, reduced-motion respected) — removing the package from package.json/lockfile — rather than fighting or patching the dependency. Confidence: 0.75

- For library books, wants an interactive page-flip flipbook reading experience (turning pages like a real book) applied consistently to every book — extends the earlier "actual printed book" presentation preference with interactivity. Confidence: 0.7

- Prefers third-party tracking/analytics integrations (GA4, Meta Pixel, etc.) to be loaded centrally from the root layout file rather than added to each page file. Confidence: 0.9
- Prefers loading third-party scripts with Next's `next/script` component using `strategy="afterInteractive"` so tracking loads without blocking the main thread. Confidence: 0.9
- Prefers third-party service/tracking IDs (GA4 measurement ID, Meta Pixel ID) to be kept out of source code and injected via `NEXT_PUBLIC_*` environment variables (e.g., `NEXT_PUBLIC_GA_ID`, `NEXT_PUBLIC_META_PIXEL_ID`) rather than hardcoded — distinct from site config/content, which they wanted file-based. Confidence: 0.8
- When implementing integrations, expects the delivered response to include a clean code snippet of the updated file(s) and a sample `.env.local` configuration block. Confidence: 0.6

- Dislikes decorative grid/dot background patterns on navy page heroes; wants the standard page hero to be a clean solid navy block with a breadcrumb trail, gold kicker, display headline, and descriptive subtext — explicitly asked to remove the grid from the "navy grid hero" variant and add breadcrumbs + subtext instead. Confidence: 0.7
- Treats gold (`--vil-gold`) as the brand's "secondary color" and wants CTAs on dark/photo heroes to use the golden treatment (gold background, navy text) rather than the navy primary button — explicitly asked for the photo hero's CTA in golden "secondary color". Confidence: 0.7

- For error pages (404), explicitly wants a full-page standalone layout — full viewport (`min-h-screen`), centered content, no site shell/header/footer wrapper — rather than embedding the error page within the site chrome. Confidence: 0.6

- Prefers matching the logo asset variant to the background tone: the colored (navy/gold, non-white) VIIV logo on light/white backgrounds and the white logo variant on dark/navy backgrounds — explicitly asked for the colored logo on the white 404 page. Confidence: 0.7

- Delegates copywriting and content extrapolation when they give a loose vibe: when the user phrases page copy as a suggestion (e.g., "~ something corny like that" for the 404 tagline) or summarizes a process with "~ extrapolate this process, and put relevant content" (the how-to-apply page), they expect the agent to draft on-brand copy AND expand the bare summary into complete page content themselves — numbered steps with plausible durations, a "what happens next" timeline, eligibility points, FAQ answers — rather than ask for exact text or more specifics. The "never fabricate" rule covers real facts/data (addresses, stats, testimonials), not marketing copy or process framing the user explicitly invited. Re-confirmed when the user pasted draft community-page copy and said "~ based on this, write more sections" — the agent built out the full page (by-the-numbers strip, journey stages, culture cards, closing CTA) from that seed text without requesting more details. Confidence: 0.75

- Wants interactive flipbook navigation to reuse the hero section's circular nav language — a round ring button with left/right arrow icons (gold fill on hover) — and the buttons positioned far outside the book (vertically centered, clear of the book edges) rather than the library's default arrow buttons. Confidence: 0.8

- Considers a third-party component's default effects undesirable when they clash with the site aesthetic — e.g., page-flip's dramatic shadows and ghosted/transparent pages during flips — and expects them fixed via the library's configurable props (drawShadow, maxShadowOpacity) plus CSS overrides that force opaque page backgrounds and cap shadow opacity, rather than accepting the defaults or patching library source. Confidence: 0.7
- For new decorative motion/animations (e.g., an idle floating animation on the WhatsApp button), wants the site's existing `prefers-reduced-motion` block extended to disable the new animation too — the float was explicitly added to the reduced-motion block, not left running for motion-sensitive users. Confidence: 0.6

- Prefers a single canonical home for design/component reference: when `/ui-kit` was superseded by `ui-guide/components`, wanted its full contents moved over (preserved verbatim, not recreated) and the old route decommissioned with a redirect to the new home — rather than keeping parallel/duplicate reference sections. Confidence: 0.8

- Wants every item in a navbar/menu to have its own dedicated route — explicitly asked for a separate route per "Campus life" menu item instead of multiple menu links all pointing to a single shared page (e.g., `/campus`). Each menu entry gets its own URL/page. Confidence: 0.8

- For pages/routes that aren't built yet, prefers a single reusable branded "coming soon" component applied to all of them (each page passing its own title/description and a back CTA) rather than stub or empty pages — explicitly asked to "create a coming soon component and put it on all of those pages" — re-confirmed when asked to "put coming soon" on the admissions sub-pages. Confidence: 0.85

- When assigning routes to menu items, expects already-existing routes to be kept as-is (e.g., "book a tour" and "student guide" already have routes) and only new routes created for the remaining items — don't overwrite or duplicate existing pages. Confidence: 0.7
- Wants the navbar/header's initial state to be route-dependent: it should start in transparent/white (hero) mode only on the home page route, while every other page should start immediately in the default (solid/scrolled) colors — no transparent header on interior pages, regardless of scroll position. Confidence: 0.8
- For visitor-facing location/directions pages (e.g., Campus > Location), explicitly wants a complete experience rather than a minimal stub: a Google Maps embed that drops the pin at the exact campus location, point-to-point journey routes from major transport hubs (railway station, airport, bus termini) with exact travel time, distance, via route, and numbered steps, step-by-step navigation instructions, and a prominent notice that first-time (non-student) visitors must book ahead with a CTA to the booking page — while students/returning visitors can walk in. The generic "getting here by common transport" modes grid was explicitly removed as redundant once the hub-to-hub routes existed. Confidence: 0.85
- Wants the footer CTA box (the pre-footer conversion section, normally "Earn the degree. / Build the venture." + Apply Now) to be page-specific rather than a fixed global message: each page should be able to replace it with a CTA tailored to that page's primary action (e.g., the Location page uses "Plan your visit / First-time visitors must book ahead" with a "Book a Campus Visit" button), reusing the page's existing content data for the override. Confidence: 0.7
- Prefers no redundant/duplicate conversion CTAs on a page: when the page-specific footer CTA already carries a message (e.g., "First-time visitors must book ahead"), a standalone CTA card placed above the footer repeating that same message should be removed so the page ends cleanly without stacked/repeated CTAs. Confidence: 0.7

- Wants generated/derived assets (e.g., Open Graph images) to be versioned by build datetime in the filename (e.g., `og-20260806-043549.png`), so every build produces a brand-new file and social platforms never serve a stale cached image — and wants a config mechanism controlling that versioning (env override such as `OG_BUILD_STAMP`, with an "off" toggle to disable versioning) rather than a hardcoded value. Confidence: 0.8
- Wants artifact generation automated through git hooks: a pre-push hook that regenerates the versioned assets, commits them with a dated message, and pushes them so generated files are always up to date — expects the hook to include clear skip/disable escapes (`git push --no-verify`, a disable flag file/env var) and to stage only the generated artifacts plus their config, never unrelated working-tree changes. Confidence: 0.8
- Wants branded assets in generated images (OG cards, etc.) to use the real brand mark from the repo's asset files (e.g., the VIIV shield logo in `public/brand/logo_main.png`, which has a transparent background and works on light backgrounds) rather than a text-only or recreated approximation — "add the logo" means embedding the actual logo asset. The same rule extends to in-page brand visualizations: when the user specifies the center of an orbit component should be "the VIIV logo (the one in navbar)", the actual navbar asset (`/brand/logo_main.png`) is embedded rather than a recreated mark. Confidence: 0.7
- Prefers the brand display font for OG/social images: when the site theme calls for a specific display serif (e.g., Fraunces) and the repo has no local font files, expects the OG route to fetch the real font from Google Fonts at generation time (matching weights actually used in the card) rather than falling back to system fonts. Confidence: 0.7
- For OG image generation, wants the component to use only the font weights actually loaded for Satori (e.g., 600/700), adjusting the card's declared weights to match, rather than declaring weights no font data exists for. Confidence: 0.6
- Prefers installing UI components through the official shadcn CLI registry command (e.g., `npx shadcn@latest add @ogimagecn/shadcn-registry-3`, `npx shadcn@latest add @magicui/scroll-based-velocity`, `npx shadcn@latest add @magicui/orbiting-circles`) rather than hand-writing or hand-copying them — explicitly dictates the exact CLI command for adding new registry components. Confidence: 0.8
- Expects automated generation scripts and git hooks to be robust to the developer's real working state: when a dev server is already running, the script must detect and reuse it (via the lock file or an explicit port override) or pick a genuinely free port, rather than spawning a duplicate that dies on a port conflict and breaks the push — reported a failed `git push` caused by the pre-push OG script colliding with their running dev server on port 3000. Confidence: 0.7

- Prefers global, app-wide conversion UI: a single large modal mounted once at the site-shell level and triggerable from anywhere via a React context provider + hook (e.g., `openLeadModal()` / `useLeadModal()`), rather than per-page local modal instances — explicitly asked for a modal "triggered from anywhere in the app." Confidence: 0.6

- Wants open/modal animations to fade in every text element individually, one by one — each logo, headline, paragraph, list-item title/body, note, and form field gets its own staggered motion item — rather than animating grouped blocks together; explicitly clarified "each text item... should fade in one by one. Each component." The stagger must also play on the initial modal load (first mount), not just on later stage transitions — when the modal opens static/invisible animation, it is reported as a bug (root cause was `AnimatePresence initial={false}` suppressing the first-mount animation). Confidence: 0.85
- Wants modal stage-to-stage transitions to be animated (cross-fade/scale between steps) with spring micro-interactions (e.g., success icon popping in with scale+rotate), rather than a static or default-fade dialog. Confidence: 0.85
- Prefers lead-capture/conversion flows to be multi-stage wizards rather than single-submit forms: stage 1 collects contact details (name, phone, email), stage 2 verifies the phone via OTP ("We've sent an OTP to {phone}"), and stage 3 is a success state with confetti that personalizes the confirmation and sets expectations for what happens next (e.g., a career counselling call within 48 hours). Confidence: 0.6
- For phone-verification OTP entry, wants the standard 6-digit code with roomy, well-padded input boxes (taller/wider slots, visible gaps between them) rather than a cramped 4-digit layout — called out 4-digit boxes as lacking spacing/padding and then asked again for even more spacing ("add MORE spacing") on a later pass, so the boxes should err on the roomy side. Confidence: 0.7
- Frames lead-capture copy as an application process ("Start the application process") and wants the success screen to deliberately feel like selection/admission — "Application received", "Congratulations, {firstName}!", celebratory confetti, and next-step cards about admission guidance — giving the aspirational impression that the applicant has been accepted/selected, even before any real admission decision. Confidence: 0.8
- For low-commitment lead magnets (gated downloads like the student handbook), prefers a much simpler capture form than the full application wizard: just name + email, no OTP, no phone, no recaptcha — explicitly asked for a form "like the apply now form, but much more simpler" and expects it styled to match the existing form/tokens. Confidence: 0.8
- For premium/long-form content (library books), wants a free-preview + gate pattern rather than capturing upfront: let the reader flip through the first few pages, then blur the entire component (pointer-events off) past a fixed page threshold (e.g., after page 3) with a "Download this" CTA that opens the lead-capture modal — one-way gate, not repeated nagging. Confidence: 0.7

- Treats production build warnings as defects to fix rather than acceptable noise: pasted the full `npm run build` output showing a Turbopack "Encountered unexpected file in NFT list" tracing warning (unbounded `join(process.cwd(), ...)` fs reads in `og.config.ts`/`og/route.tsx`) and a missing-`metadataBase` warning with just "fix this", expecting the build to complete with zero warnings — not merely exit 0. Wants build-time filesystem reads scoped to their known subfolders (the `/* turbopackIgnore: true */` pattern) and `metadataBase` set to the production domain so the output is fully clean. Confidence: 0.6

- Wants push automation (git hooks/scripts) to never hang or race: when a hook runs an inner `git push`, it must be fully non-interactive (`GIT_TERMINAL_PROMPT=0` with stdin from `/dev/null` so a missing credential prompt fails fast instead of blocking with no TTY) and use `--no-verify` so the hook doesn't recursively re-enter itself and race on the ref lock — plain `git push` must "just work" (reported a pre-push hook that got stuck at its inner push waiting for GitHub credentials, with nested pushes silently failing). Confidence: 0.8
- When a git hook (e.g., a custom pre-push hook that auto-generates/commits OG images and runs an inner push) blocks the developer's actual ability to push, prefers the hook removed outright over keeping or repairing the automation — explicitly asked "Remove the pre push hook, i'm unable to push only", and the fix is simply deleting it from `.git/hooks/` so plain `git push` works again. Push reliability trumps the convenience of automated artifact generation. Confidence: 0.7
- Wants the site's single source of truth for real-world contact data (e.g., the admissions WhatsApp phone number in `admissions.config`) reused when wiring new UI, rather than re-hardcoding the same number in the new component — the WhatsApp FAB pulls its number from `admissionsConfig.admissionsPhone` and strips non-digits for the `wa.me` link. Confidence: 0.65
- Prefers a wrapper script (`npm run push`) as the clean single-command "generate → commit → push" path: because a pre-push hook's own commit can't be included in the same push (git resolves the ref before the hook runs), the wrapper does the generate+commit itself then pushes with `--no-verify` to avoid re-triggering the hook — recommended over relying on the hook alone for a fully clean flow. Confidence: 0.7

- When the user reports a change "can't be found" in the UI, treats the live dev server's served output as the source of truth: the agent fetched the running server's HTML, confirmed the new link was actually rendered, and concluded the user was seeing a stale browser cache or the production deployment — the resolution is a hard refresh or a deploy, not a code change. Confidence: 0.7
- When browser-side code must call a cross-origin backend that lacks CORS headers (e.g., an n8n webhook in `webhook-test` mode that doesn't answer the CORS preflight), prefers routing the call through a small server-side Next.js API-route proxy — the browser posts same-origin to the proxy, the server forwards to the third-party origin, and the response (normalized from array-style to a plain object) is relayed back — rather than attempting cross-origin fetches or asking the backend to add CORS headers. Confidence: 0.8
- When the client calls a webhook/backend, wants success/failure decided by the response payload (e.g., `verified: true` or `route: "verified"`) rather than the HTTP status alone, and errors mapped to user-facing messages. Confidence: 0.7
- For secrets used in server-side webhook code (e.g., an OTP pepper in an n8n Code node), wants them read from the environment (e.g., `process.env.OTP_PEPPER`) and kept out of the source/workflow itself — explicitly warned that anyone who can view the workflow can see a hardcoded value ("Change this to a long random secret. Anyone who can view this workflow can see it."). Confidence: 0.7
- When the demo OTP flow used a hardcoded demo code, asked to wire it to a real n8n webhook (request the OTP, send it via WhatsApp, verify through the webhook) rather than keep the simulation. Confidence: 0.7

- Likes a stateless, salt-based OTP/verification scheme: the client generates a random salt and submits it with the form data to a webhook; the backend derives the OTP from the salt and delivers it (e.g., via WhatsApp); verification submits the entered OTP back with the salt so the backend can check it — explicitly described the scheme as "simple, yet effective." Re-confirmed when the user shipped a full stateless n8n implementation that recomputes the OTP from phone + salt + time bucket with no stored state. Confidence: 0.8
- When the client and backend share a stateless OTP scheme, wants the salt→OTP derivation algorithm spelled out concretely and implementably (deterministic derivation from the salt keyed by a server-side secret via HMAC-SHA256, so the OTP can be recomputed for verification without storing anything, plus optional time-bucket expiry) with ready-to-drop-in code for their backend (e.g., an n8n Code node) — explicitly asked "how to calculate OTP from salt? Tell that algorithm" rather than accepting a hand-wavy explanation. Re-confirmed when the user implemented the HMAC-SHA256 + time-bucket derivation end-to-end in their n8n Code node. Confidence: 0.7
- When integrating with an async/fire-and-forget backend (e.g., an n8n production webhook that returns `{"message":"Workflow was started"}` immediately and runs the workflow in the background), prefers the synchronous fix — configuring the backend webhook to "Respond to Webhook" and return the verification result in the same request — over polling a second endpoint or moving verification client-side; chose this option when offered so the frontend keeps a single request/response flow. Confidence: 0.6
- When an external-service-backed flow misbehaves (e.g., the user asserts "NEXT_PUBLIC_OTP_WEBHOOK_URL=... this is correct. The workflow is live... why is it not working? Fix it." or reports a UI error like "it says 'OTP didn't match'"), expects the agent to debug by testing the live endpoint directly (e.g., curl against the production webhook) and inspecting the raw response to find the real root cause — not to re-verify the config, second-guess the user, or change app code while the backend is still suspect. The root cause is frequently the external workflow's wiring (e.g., an unconnected "Respond to Webhook" node making n8n return HTTP 500 "Unused Respond to Webhook node found in the workflow") rather than a frontend bug. Confidence: 0.75

- Prefers reverting to a previously-agreed simple/deterministic design over iterating through increasingly complex workarounds: when an integration fix drags the user into manually rewiring external services and repeated test cycles, they want to stop and go back to the design that works without the external dependency — explicitly asked to "Revert back, I don't want to go back and forth" rather than keep troubleshooting the external-service path. Confidence: 0.8
- Prefers marquee/velocity-scroll sections (e.g., scrolling mentor rows) to have rows moving in opposite directions at the same speed — explicitly asked for "2 rows of mentors, scrolling at the same velocity in the opposite direction" — implemented by giving both rows the same `baseVelocity` and mirroring `direction` (1 / -1) in the shared `ScrollVelocityContainer`. Confidence: 0.85
- For marquee/velocity-scroll sections, at one point wanted the scroll to pause on hover so cards could be inspected (with the pause being section-wide — hovering anywhere must pause ALL rows through a single shared hover state; a row-local pause was reported as a bug). This interaction was later explicitly removed for the mentor cards: the user asked to "remove the onclick effect or hover effect. It shouldn't pause at all" — so the mentor velocity-scroll is now purely presentational: continuous scrolling with no pause-on-hover and no click-to-open on the cards. Confidence: 0.9
- For marquee/velocity-scroll sections, prefers a slower, calmer scroll speed — explicitly asked to "reduce scroll speed" on the mentor velocity-scroll rows (baseVelocity cut from 5 to 2.5 on both rows) rather than leaving the default/faster velocity. Confidence: 0.5
- Wants the gold-on-hover treatment for circular icon elements to apply beyond the flipbook nav: on the mentor velocity-scroll cards, the ArrowUpRight badge should fill gold (gold background, navy icon) on card hover, keeping complementary micro-interactions like the icon rotating — confirming gold-fill-on-hover as the site's hover language for circular icon badges. Confidence: 0.7
- Wants card designs and their hover interaction patterns to carry across sections as one consistent card family: when introducing a new card type (e.g., mentor cards in the velocity-scroll section), it should be "like the bento grid cards" — the same card look plus the bento glare/shine-sweep on hover — rather than a new, distinct card treatment; established interaction language (glare on hover, click-to-open profile dialog) is expected to transfer to the new cards. Confidence: 0.8
- Removes deprecated brand/partner/institution names sitewide: when told "wherever we've mentioned X, remove it", expects every occurrence gone — content data files, components, pages, and even demo/ui-guide references — replaced with generic on-brand phrasing that preserves the benefit claim (e.g., "recognized online BBA" / "university degree pathway" instead of naming the university), not just removed from the index page or left dangling anywhere. Confidence: 0.75
- When the user explicitly caps the scope of a brand/term removal by naming the exact places (e.g., "From the about page, footer. Rest all places, let it be there."), the change must be limited to exactly those spots — every other occurrence of the same string (legal pages, config data, comments, UI-guide references) is deliberately left untouched, even when the named and untouched spots render from the same config field. The agent removes only the render blocks at the named locations (e.g., the parent-org link line on the about page and in the footer) and does not propagate the removal or clean up the now-unused shared config entries unless asked. Confidence: 0.8
- Wants visual representations of real items to be the actual item, not decorative stand-ins (e.g., book covers on the index page shouldn't use campus photos; the About page's story section should show the founder's actual photo rather than a placeholder component). When the real assets don't exist in the repo, accepts being asked and opting into generated branded placeholder covers (title/author/label on a branded design) as an interim — while flagging that the real cover files can be dropped in later — rather than keeping unrelated imagery. Re-confirmed when the user tersely asked "Use the founder's picture in the about section" and the story section's ImagePlaceholder was swapped for the real founder portrait in a rounded card. Confidence: 0.75

- When asked to remove a page/route, expects a complete and consistent removal: the page itself plus every inbound reference to it sitewide — nav, footer, hero/header CTAs, buttons, breadcrumbs, back-links, content config files, dead anchors, AND any content sections tied to it — either repointed to the surviving replacement or deleted outright. Explicitly phrased as "remove this page, and any links to it" (e.g., removing `/admissions#apply`) and re-confirmed with "Remove both of these routes ... wherever it is mentioned, remove those buttons / links / sections" for the `/program/graduation-and-demo-day` and `/program/build-an-enterprise` routes. Confidence: 0.9

- Prefers surgical, minimal-scope removals: when a requested page removal has sibling sub-pages, removes just the referenced page (not the whole section), keeps the sub-pages intact, and repoints their backHrefs/breadcrumbs to the surviving main page — chose "just remove the landing page, keep the 3 sub-pages" over deleting the entire section when asked to clarify scope. Confidence: 0.7

- When the user says a new component should be "like" an existing one (e.g., mentor cards "like the bento grid cards"), that means visual inspiration only — take the design language (palette, glare/shine effect, card interaction patterns) but do NOT reuse/import the existing components themselves; reusing the bento components for the mentor cards was explicitly rejected: "Only thing I want you to take from the bento cards is, inspiration, no reusing components for this task." Confidence: 0.9
- When debugging one failure, expects already-verified/working code to be left completely untouched — e.g., explicitly said "The phone validation fix is confirmed working... Do not modify the now-working phone normalization logic" while a separate OAuth failure was investigated. Fixes must be surgical: only the specific failing part changes, and confirmed-good logic is treated as out of scope. Confidence: 0.6
- Expects the agent to verify a completed task against the real rendered output before reporting done ("Once done, verify if it's done or not") — an end-to-end check such as a headless-browser click-through (cards render, dialog opens, links resolve, dialog closes) rather than relying on lint/build passing or inspecting code alone. Confidence: 0.8

- For person/profile cards (e.g., mentor cards in the velocity-scroll section), prefers the dark gray/navy treatment featuring the person's portrait photo and the company logo — explicitly said "I liked the colors earlier, gray / dark card, with the picture of the mentor, logo of the company" and asked to revert to that styling when the card had been restyled as a light/gold card. Confidence: 0.85
- Wants profile dialogs for experts/mentors to cover both the subject itself and the person's credibility — structured as labeled sections (e.g., "The subject" and "Why they teach it", backed by `about` + `why` data fields) rather than a single bio paragraph, so clicking a card explains the topic AND why that specific person is the best fit to teach it. Confidence: 0.8
- For tasks the user deems simple, wants fast execution with minimal ceremony — explicitly said "Don't take too long, it's a simple task. Quickly finish it." and, when the agent stalled mid-fix, "Fix it quick. You're taking too much time." — so small changes should be implemented briskly rather than run through extended deliberation, repeated file re-reads, or drawn-out verification loops. Re-confirmed with strong emotion when the user angrily reported the session "running for fucking 1 hour" and "ate my tokens" — they are highly sensitive to both elapsed time and token consumption, so long-running tasks, hung commands, and repeated verification/rebuild loops are unacceptable; work fast and cheap, and don't let commands hang. Re-confirmed again when the user pressed "Why are you taking so much time? Do it quickly." mid-implementation of the callback-dialog feature, with the agent responding by compressing the plan and executing in one pass — even a moderately-sized feature must not read like it's dragging. Re-confirmed yet again when the user cut short a small regression fix (vanished icons on the orbit rings) with "Try again. You're wasting too much time." — even a straightforward bug fix should be executed decisively and swiftly, not stretched out with repeated file reads and deliberation before the first attempt lands. Re-confirmed once more when the user re-sent the same bug report with an explicit time constraint baked into the instruction itself ("Fix it. Don't waste too much time, quickly fix it.") — the expectation is a fast diagnosis (check the served output, read the suspect component once), a surgical edit, and a quick verify, with no drawn-out investigation. Re-confirmed again mid-task when the user cut short the life-at-viiv gallery work with "You're taking too long. Do it FAST!" — the impatience applies to any in-flight task, not just bug fixes, so multi-step feature work (e.g., reading many files up front before acting) must also move briskly. Re-confirmed once more when the user re-sent the same gallery request with an explicit time constraint baked into the instruction itself ("Don't take too long, the images' name already describes what it is. Do the task quickly.") — even a task the user considers trivial/self-explanatory should be executed in one fast pass, with minimal up-front reading. Confidence: 0.99

- For third-party integrations that require credentials (CRM, payment, etc.), mandates a strictly server-side architecture: the browser never calls the third-party directly and never holds secrets — it posts to a local Next.js API route, which reads the credentials from env vars and calls the provider server-side. Explicitly specified "The browser must NEVER directly call Zoho CRM", "Zoho credentials must remain server-side", no `NEXT_PUBLIC_*` exposure, no hardcoded credentials, and no new database or third-party automation service (n8n etc.) added — the app's own API route is the only bridge. Confidence: 0.9

- Wants third-party API field mappings (e.g., form fields → Zoho CRM Lead API field names) isolated in a single, clearly separated mapping object with a comment marking it as the single source of truth where custom field API names can be added/overridden later — so swapping or extending provider field names never requires touching the rest of the code. Confidence: 0.8

- For third-party API integrations, wants success determined by checking BOTH the HTTP status and the payload's per-record status (e.g., Zoho's `code === "SUCCESS"` plus a record id), with full provider error details logged server-side but only a clean, generic JSON (`success`/`message`, optionally an id) returned to the browser — never exposing access/refresh tokens or unnecessary provider internals. Confidence: 0.8

- Wants client-side submission handling that keeps the form populated unless the submission actually succeeded (no clearing on failure), shows explicit loading states per stage, and blocks duplicate submissions while a request is in flight. Confidence: 0.7

- When implementing a form→third-party integration, expects the final response to state exactly which form fields were mapped to which provider API fields (e.g., a name/phone/email → First_Name/Phone/Email mapping list), so the mapping is auditable without reading the code. Confidence: 0.7

- When establishing/fixing an integration flow (e.g., lead submission to Zoho CRM through `/api/lead`), expects ALL lead-capture instances across the codebase to be audited and wired to that same flow — including less obvious ones like gated book-download forms and event signup forms (bootcamp/webinar pages), not just the main form — explicitly asked "Check if all lead forms (even book pages had some) have this flow?" and expects leftover stubs (e.g., a fake-success TODO timeout in `SignupForm`) to be replaced with the real call, matching the established pattern (duplicate-submission prevention, real error handling, distinguishable `source` value). Confidence: 0.8

- When handed a third-party snippet (e.g., a Meta Pixel code from the marketing team) and told scaffolding already exists, expects an audit-first workflow: inspect the existing implementation (components/layout), the `.env*` files and gitignore/git-tracked state before writing anything; then make only the missing changes (e.g., swap a placeholder pixel ID for the real one in `.env.local`), verify with a real build, and report exactly what was already in place, what changed, and whether it's ready to commit and push — explicitly flagging that gitignored env edits stay local and the same variable must be set in the deployment/hosting environment for production to work. The commit/push step itself is left to the user ("let me know once this is ready to be committed and pushed"), not performed by the agent. Confidence: 0.65

- When investigating a failure, expects the agent to avoid CLI commands that mutate local state (e.g., running `vercel link` rewrote `.env.local` with a `VERCEL_OIDC_TOKEN` line and created a `.vercel/` folder) — the user flagged this as going too far. If side effects do occur, they must be restored exactly (added tokens removed, created folders deleted) so the local environment is untouched apart from the intended code change; investigation should stop once the root cause is confirmed rather than continuing to poke around and accumulate more side effects. Confidence: 0.7
- When a Tailwind v4 project's custom animation stops working (e.g., `animate-orbit` chips vanishing from the page), expects the agent to verify the animation actually compiles into the served/compiled CSS — the `.animate-*` utility and its `@keyframes` must both be present in the built stylesheet. A known root cause: tooling (e.g., the shadcn CLI) inserts `--animate-orbit` AND a nested `@keyframes orbit` INSIDE the `@theme inline { }` block; Tailwind v4 then emits the utility but silently drops the nested keyframes, so the class references keyframes that don't exist and the animation never runs (elements positioned off-center by the transform sit invisible). The fix is moving `@keyframes orbit` out of the theme block to top-level CSS — confirmed by checking the served page's compiled CSS for both `animate-orbit` and `@keyframes orbit`. This also validates the user's assumption that a prior change ("you messed something up") broke the feature even when the underlying cause is tooling-modified CSS rather than the agent's own edit. A second, distinct root cause for the same symptom is a stale dev-server CSS cache: the source `styles.css` is correct AND a clean production build emits both the utility and the keyframes, but the running dev server's compiled CSS doesn't — the fix is a clean dev-server restart (kill the port / `rm -rf .next`), not a source edit, and a fresh build then confirms the animation renders. When the orbit logos "vanish" again, the served HTML still contains all the logo `<img>` tags, so the diagnostic is always the compiled CSS, never the markup. Confidence: 0.75
- When a verification step (e.g., OTP) is deemed unnecessary or a blocker for lead conversion, wants it temporarily disabled so form submission creates the lead directly (name/phone/email → `createLead` → success screen) while keeping the existing validation/recaptcha and the same API path — a minimal, easily-reversible change to the flow, not a redesign of the form. Confidence: 0.6
- When asked to investigate a failing API ("investigate why and tell me clearly"), wants a plain-language root-cause report that separates what works locally from what fails in production (e.g., lead creation succeeds on the local dev server and in direct Zoho calls, but returns 502 in production because the `ZOHO_*` env vars are missing from the deployment), states the evidence, and names the exact remediation — not a code-level walkthrough or speculation. Confidence: 0.6
- When reporting a production failure with a screenshot of the hosting platform's UI (e.g., a Vercel env-var dialog for failed lead creation on PROD), explicitly directs the agent to the detailed console/function logs for the real error ("The console error should be detailed") — the diagnosis must come from the actual logged error (pulling Vercel logs / testing the live endpoint), not from reasoning about the surface-level screenshot alone. Also expects production error logging itself to be detailed enough to support this diagnosis. Confidence: 0.5
- For production troubleshooting on Vercel, expects the agent to verify which Vercel project actually serves the live domain before concluding anything about env vars: env vars configured on the linked/local project do not apply to a different project that owns the domain (the `ZOHO_*` credentials lived on `viib-landingpage` while `viivindia.com` was served by `viib-india`, which had none — the actual root cause of the 502). Diagnostics must check the deployment/project ownership of the domain (`vercel ls`/`domains ls`/logs revealing the serving project) and apply the fix (adding env vars, redeploying) to the serving project, then redeploy so the running deployment picks up the new vars. Confidence: 0.5
- For legal/terms links attached to forms (Terms and Conditions, Privacy Policy), wants them to open in a new tab (`target="_blank" rel="noopener noreferrer"`) so users don't lose their form input — and expects the change applied to every form carrying such links across the codebase, not just the main form. Confidence: 0.65
- When a nav link/section is temporarily out of commission, prefers it commented out in place (the whole entry commented so it's trivially re-enabled later) rather than deleted — explicitly asked to "comment out" the Demo Days nav link, meaning hidden from the menu while the code stays in the file. Re-confirmed with fresh evidence when the user said "Just hide it for now" for BOTH the Campus Life "Resources" nav panel and the homepage "The reading every builder starts with." section — expects non-destructive hiding (data emptied with a "temporarily hidden — restore items" comment, render logic guarded to show nothing) that keeps the code restorable, never actual deletion. Re-confirmed again when the ForStudents homepage section was hidden by commenting out its import and JSX with a "temporarily hidden — restore below when ready" note. Re-confirmed yet again when the user said "In /program, remove the 'demo day' section (hide it for now)" and the agent commented the whole section out in place with a "temporarily hidden. Restore when ready" note (also removing the now-unused data import so lint stays clean) — "hide it for now" always means keep the code restorable, never actual deletion. Confidence: 0.9

- For the WhatsApp FAB / conversion buttons, prefers an in-page chat-widget dialog ("like a chatbot") over deep-linking straight to WhatsApp: the FAB toggles open a chat panel with a welcome bubble, quick-reply chips, a free-text input, and a call fallback, and selecting a chip or typing a message hands off to WhatsApp with a prefilled message in a new tab — rather than the button jumping directly to `wa.me`. Explicitly asked "The FAB should open up a dialog, like a chatbot." Confidence: 0.7

- When the user supplies exact replacement copy for headlines/subcopies (often pasted together with the element's DOM path and rendered HTML), expects it applied verbatim with their exact wording — even when the quoted text differs from what's actually in the source files, the user's version wins and replaces the old text wholesale (e.g., pasted subcopy for a hero slide was applied even though the codebase had different wording). Re-confirmed when the user pasted new /program hero copy ("Learn. Build. Launch. Scale." + the "challenge-based venture-building journey ... UGC-recognised Online BBA" description) with only a page/element location and the source's existing body copy was replaced wholesale by the pasted version, em dash and all. Re-confirmed again when the user pasted verbatim copy for the bento "Recognized Degree" card (title changed to "Recognized Online BBA Degree" plus three detail paragraphs about the KARE partnership) with only a card location given — the card's `short` text and modal `details` were both replaced wholesale with the supplied wording. Re-confirmed again when the user pasted a "Career & Venture Outcomes" bento block (new card title plus four paragraphs) and the card's title, `short` text, and modal `details` were all replaced wholesale with the supplied wording — including the title rename and the "Day One" capitalization and em dashes preserved verbatim. Confidence: 0.85
- When the user hands over full copy for an existing page and says "redesign it fully as per the copy," expects the entire page restructured around that copy — the copy's own structure (hero headline → story → mission → closing tagline, e.g., the About page's "India needs more builders. / We're building them." → why-we-exist → mission → "IGNITE. BUILD. LAUNCH.") becomes the page's section map, with the old layout fully replaced rather than patched section-by-section — while building the new sections from the site's established design system (SectionShell tones, Reveal animations, viiv-kicker/viiv-section-title, --vil-* tokens, ApplyButton, icon cards) rather than inventing a divergent look. This explicit "redesign fully" instruction overrides the usual preserve-and-extend default for page updates. Confidence: 0.6

- When headline copy changes, wants the key emphasis words kept highlighted with the site's established treatment (e.g., one phrase underlined in gold, another in gold text, as in the status-quo h2) — and explicitly delegates the choice of which words to emphasize rather than specifying them ("highlight the right words"). Confidence: 0.6

- Requests UI/copy changes by pasting the exact DOM path and rendered HTML of the target element from devtools (e.g., `div.relative min-h-screen ... > h1.whitespace-pre-wrap ... aria-label="..."`), expecting the agent to locate and change precisely those elements rather than guessing from descriptions. Confidence: 0.6

- When the user asks for a codebase-wide audit of a brand/term (e.g., "IN the entire codebase, where all have I used the word 'Varman Innovation Labs'? Give me a list."), expects an exhaustive inventory covering the whole repo — every occurrence with file and line (content data, legal pages, comments, UI-guide references, not just src), and the occurrences categorized as data-driven (single source of truth that updates everywhere) vs hardcoded copy, so the blast radius of an upcoming rename/removal is clear without further digging. Confidence: 0.6
ons. Confidence: 0.6

- For narrative/milestone content (e.g., a multi-year program roadmap), wants it built as a scroll-driven "journey map": a progress path that draws itself as the user scrolls, with timeline nodes and cards animating in on viewport entry — explicitly asked for "animate on scroll effects, as if the user is travelling" — rather than a static timeline/list. Confidence: 0.65
- When commissioning a new section, delegates the full design and implementation from a concept plus exact copy: the user supplies the section's vibe/behavior (e.g., "a journey map, which has animate on scroll effects, as if the user is travelling") and the verbatim content blocks (year, theme, tagline, body, outcomes), and expects the agent to design the layout, animation, and data structure itself without asking for more specifics. Confidence: 0.6
- Explicitly forbids emojis in site content/copy: when pasted content contains emoji glyphs (🚀 🎙️ 💼), they must be replaced with icon components (lucide-react icons: Rocket, Mic, Briefcase, etc.), never rendered as emoji characters — "Don't use any emojis, use only icons." Re-confirmed when the user pasted the Life-at-VIIV copy block containing 🏆 and 💃 section markers and the shipped section used lucide Trophy/Music icons instead of the emoji glyphs — the icon-replacement rule holds even when the user's own pasted copy contains the emojis. Confidence: 0.95
- When asked to re-arrange/re-design a whole page for reading flow, expects redundant legacy sections to be replaced rather than stacked: the old section equivalents (e.g., a static year-cards list and an overlapping venture-paths section) get removed so the new narrative (journey map → path choices → demo day → graduate-with proof) reads as one coherent story — page copy stays exactly as supplied. Confidence: 0.6
- When the real content for a section isn't ready yet and the user asks for a "for now" version (e.g., "for now, just put that the UGC recognized degree is provided by Kalasalingam University ~ images"), prefers a minimal truthful presentation over fabricated mockups: strip placeholder fake visuals (mock certificate cards with a fake "Student Name") and state the simple real fact, featuring the real official assets (the university's actual banner/logo from `public/`), while keeping the placeholder data in the content file so it can be restored later when real assets arrive. The terse "~ ..." phrasing signals the user's loose intent (here, "include the images") and expects the agent to fill in the presentation details without asking. Confidence: 0.6

- For co-branded/partnership panels, wants both logos to carry equal and substantial visual weight — explicitly rejected a layout where the brand's logo/wordmark appeared tiny next to a partner's banner ("This looks UGLY! Re-design it. The VIIV logo and wordmark is too small") and expected the logo enlarged roughly 2.5x so the brand has real presence, presented as a balanced two-column panel (logo | "in partnership with" divider | partner logo) rather than a small badge squeezed beside a banner. Confidence: 0.7
- In co-branded/partnership panels, expects the mobile (stacked) layout to mirror the same balance: the panel stacks into a single centered column with both logos at the SAME size and the "in partnership with" divider text centered — reported a mobile screenshot where the logos weren't centered and differed in size, and the accepted fix was `flex flex-col items-center` + `text-center` on mobile with both logos given the same fixed height (`h-24`) while the desktop 3-column grid stayed unchanged. Confidence: 0.7
- Avoids duplicate/oversized brand imagery within one section: a full-width university banner on top PLUS the same logo inside the partnership panel was deemed redundant and removed — the partner's branding should appear once, in a contained, balanced panel, not dominate the section twice. Confidence: 0.6
- When a logo asset already contains the wordmark (e.g., `logo_full.png`), considers re-adding the name/expansion text beside the image redundant and expects it removed — the "VIIV / Varman Institute of Venture Building" text block was dropped next to the enlarged logo, with the logo alone carrying the brand identity. Confidence: 0.6
- When an item is removed from a grid-based layout (e.g., a stat card removed from a "by the numbers" strip), expects the grid's column count to be updated to match the remaining item count so the cards fill the row evenly and stay centered — leaving a 4-column grid with only 3 items (an awkward trailing gap) was reported as a bug ("you forgot to center the divs. The 3 cards look so awkward right now") and the fix was switching to `sm:grid-cols-3`. Confidence: 0.8

- Prefers plain, audience-appropriate language in visitor-facing copy over jargon the target audience won't understand: when told "people here don't understand the word merit, so make it scholarship," expects the simpler word used in the new content AND the plain-language wording applied consistently across the page (the agent also renamed the existing "Merit Meets Need" heading to "Scholarships, decided case by case" to eliminate the flagged term everywhere it appeared) — jargon like "merit" should not linger anywhere on a page once flagged. Confidence: 0.7

- Prefers cross-linking between related pages over duplicating content: when a topic has its own dedicated route (e.g., a separate Location page under Campus Life), other pages that touch on that topic should link out to it (e.g., an "Explore the location" CTA on the Life at VIIV page leading to `/campus-life/location`) rather than restating the information in place — explicitly directed "There is a separate page for the location, it should lead to the page." Confidence: 0.6

- When handing over structured reference data to display (e.g., a tabular list of nearby spots with Category/Spot Name/Proximity/Best For columns), wants it rendered as a real data table that keeps the provided column structure and copy, with the data stored in the existing content data module (e.g., `eveningSpots` in `campusLife.ts`) following the established pattern — not reworked into cards or inline in the page. Confidence: 0.6 — note: this table presentation for the evening-spots content was later explicitly superseded by the animated orbiting-circles visualization (see the next bullet).
- Prefers presenting data-heavy content as an animated orbiting-circles visualization (MagicUI `@magicui/orbiting-circles`) over a static table: when the user said "Instead of a table, install and use this 'npx shadcn@latest add @magicui/orbiting-circles'", the location CTA became the central circle button and the data items became icon chips orbiting in two counter-rotating rings (deduplicated by name, icons keyed by category, gold/white chip styling, scaled down on mobile). The component was installed via the exact CLI command given, and the supporting data stayed in the existing content module. Confidence: 0.75
- Prefers new animated visualizations to be installed as registry components via the shadcn CLI (e.g., `npx shadcn@latest add @magicui/orbiting-circles`) and assembled into a self-contained feature component (deduplicated spots split across two counter-rotating rings with category-mapped lucide icons, the page CTA as a circular center button) with the source data still living in the established content module — then verified via typecheck/lint/build plus a live-render check before reporting done. Confidence: 0.6
- In data-driven visualizations, wants every element to be self-describing with its real data visible — when the orbiting-circles chips rendered as empty icon circles, the user flagged "the circles are empty" and specified the required format ("use icons and put what all is available around the campus, the distance. Eg: Football Court ~ 750m, Cinema Theatre ~ 800m... use the data from the table"): each orbiting item must be a labeled chip carrying the actual content (distinct icon + venue name + a small distance/proximity pill from the existing data module), not an abstract/decorative icon with no information. Re-confirmed when the icons later vanished on the campus-life/life-at-viiv rings and the user reported it as a regression to fix ("The icons vanished on the rings... Fix it.") — the icons staying visible on the orbit rings is a maintained invariant, and any disappearance is a bug to restore, not a styling choice. Re-confirmed again when the chips went missing a second time and the user sent a screenshot with "It's not there. It was there earlier, you messed something up. FIX IT!" — the invariant covers the whole visual result, not just markup: this time the DOM was fine and the root cause was the orbit animation never compiling, so the chips sat invisible off-center (see the Tailwind v4 keyframes learning below). Re-confirmed a third time when the user sent a screenshot demanding "Where the fuck did this go? No logos?" and the logos again sat invisible because the dev server's compiled CSS was stale — same invariant, different root cause (see the CSS-compilation learning). Confidence: 0.93
- When phone numbers appear anywhere in the site (header, footer, "Prefer to talk?"-style CTAs), clicking them should open a dialog (a "Request Callback" form offering Call Now / Request Callback) rather than directly dialing a `tel:` link — intercepting phone clicks with a conversion dialog, mirroring the established WhatsApp FAB pattern of opening a dialog instead of deep-linking out. Confidence: 0.6
- For time-aware conversion UI (the callback dialog), wants the dialog to check the system/browser time against business hours (9 AM–9 PM): highlight "Call Now" when within business hours and "Request Callback" when outside — but ALWAYS show both buttons ("it should show both the buttons for sure"), never hide an option based on time, only emphasize the contextually appropriate one. The callback form collects the same lead data as the existing flows (Name, Phone, Email). Confidence: 0.7
- For conversion dialogs with a time-aware recommendation, wants a two-step flow: the dialog opens on a "choice" view showing the availability context note (changes with time) plus BOTH action buttons side by side (the contextually appropriate one gold-highlighted with a ring, the other muted), and the Name/Phone/Email form is only revealed after pressing the secondary action ("Request Callback") — with a back button returning to the choice view — rather than showing the form immediately on open. The success screen keeps a direct "Call {number}" fallback. Confidence: 0.6
- When a conversion trigger exists in many places (phone numbers in header, footer, "Prefer to talk?" CTAs, the WhatsApp FAB's call fallback), wants all interception centralized in one shared component (e.g., a `PhoneLink` wrapper that prevents the default `tel:` navigation and opens the dialog) so every click point is swapped to the same single entry — and the dialog reuses the existing sitewide lead-creation path (`createLead`) with a distinguishable `source` value rather than a new submission mechanism. Confidence: 0.6
- When a photo added to a card renders too large, wants it actually resized on disk (at least ~5x smaller pixel dimensions, e.g., 1180×898 → 236×180, which also lightens the load) rather than only CSS-scaled, and displayed small and visually blended into the card — centered in its own thin-bordered section with rounded corners and slight transparency — so it sits subtly inside the card instead of dominating full-width. Explicitly phrased as "Reduce the size of that image, by 5x atleast. should be small, blended." Re-confirmed with the reverse direction: subsequent size adjustments are also given as explicit multipliers ("Make it slightly bigger, 3.5x") and expected applied to the actual on-disk file (upsized 236×180 → 826×630) as well as the display size (raised to full card width at ~26rem) — the multiplier governs both the asset and its presentation, not just CSS. Confidence: 0.75
- When a photo of people is displayed in a card (e.g., a partnership/ceremony photo), wants a caption directly below the picture naming the people and their exact roles as provided — e.g., "Dr. S. Sashi Anand, Vice President, Kalasalingam University and Mr. Arunmozhivarman, Founder, VIIV India" — explicitly instructed "Below the picture, add ..." and expected the names emphasized (bold in brand navy) within a centered, muted caption under the image. Confidence: 0.65

- When the user bans a set of image assets and designates a replacement source ("In the /photos/* folder there are a few *.jpg pictures, I don't want you to use them ANYWHERE. Instead, use pictures from /photos/early-campus/*"), expects a codebase-wide purge: every reference to the banned assets replaced with semantically-matching images from the designated folder — across content data files (campusLife, program, accommodations, student-handbook JSON), page components, and hero sections — with derived metadata (e.g., masonry gallery `orientation` values) corrected to match the new assets' actual aspect ratios so layouts stay balanced, and zero references to the banned files remaining afterward. Confidence: 0.8
- Scopes asset bans precisely (by file type or path — "a few *.jpg") and expects exactly that scope applied: non-banned assets (e.g., PNG mentor avatar placeholders) are left untouched rather than swept up, while the agent proactively flags the adjacent assets left in place so the user can decide on them. Confidence: 0.5
- For photo galleries on content pages (e.g., the life-at-viiv page), prefers a masonry-style gallery layout populated with the real photos from the designated `public/` directory (e.g., "/public/photos/life-at-viiv/*") — the photo directory acts as the source of truth for which images appear, rather than a hand-picked subset or placeholder assets. Confidence: 0.5
nfidence: 0.7
d-picked subset or placeholder assets. Confidence: 0.5
nfidence: 0.7
ry-style gallery layout populated with the real photos from the designated `public/` directory (e.g., "/public/photos/life-at-viiv/*") — the photo directory acts as the source of truth for which images appear, rather than a hand-picked subset or placeholder assets. Confidence: 0.5
nfidence: 0.7
d-picked subset or placeholder assets. Confidence: 0.5
nfidence: 0.7

- When a previously-fixed feature keeps breaking again ("How does it keep going missing? Something is messing it up. Find out the root cause, and fix that."), expects the agent to hunt for the true root cause of the recurrence rather than re-applying the same symptom-level fix — including non-code causes like version-control state. The orbit animation "kept going missing" because every orbit-related file (the orbiting-circles component, the orbit feature components, and the styles.css animation change) existed only as uncommitted working-tree changes, so any git operation that syncs to HEAD (reset, stash, checkout, or the push script's commit flow) silently wiped them; the durable fix was committing the files so the feature survives git operations, not another CSS patch. Confidence: 0.7

- When making shared/remote configuration changes (e.g., adding env vars to a Vercel project, redeploying), wants the correct target project confirmed before anything is applied — explicitly interjected mid-task with "Wait. The site is 'viib-landingpage' on vercel. Don't configure on the wrong site / project now." to halt an in-flight change, and the agent stopped the pending redeploy until the project that actually serves the domain was verified via CLI inspection; expects the agent to pause, verify ownership (domain aliases / project env) or ask directly, and get explicit confirmation before applying remote changes — acting on the locally-linked project's assumption alone is exactly the mistake being guarded against. Confidence: 0.7

- When making shared/remote configuration changes (e.g., adding env vars to a Vercel project, redeploying), wants the correct target project confirmed before anything is applied — explicitly interjected mid-task with "Wait. The site is 'viib-landingpage' on vercel. Don't configure on the wrong site / project now." to halt an in-flight change, and the agent stopped the pending redeploy until the project that actually serves the domain was verified via CLI inspection; expects the agent to pause, verify ownership (domain aliases / project env) or ask directly, and get explicit confirmation before applying remote changes — acting on the locally-linked project's assumption alone is exactly the mistake being guarded against. Confidence: 0.7
