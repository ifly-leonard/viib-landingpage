# Post-event branding assets

Drop optional branding files here for the carousel generator:

- **`branding-logo.png`** — shown top-right on intro and outro slides (recommended ~400×160px, transparent PNG)

The carousel uses the same cream/orange background and fonts as the Generate Swag poster. Intro and outro slide copy is fixed in code; only content slides are editable in the UI.
