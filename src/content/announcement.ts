/** Announcement bar content + toggle. Flip `enabled` to hide the bar sitewide. */
export const announcement = {
  enabled: true,
  message: "2026 Admissions Open — Limited Seats at the Chennai Campus | Scholarships Up to 50% Available",
  cta: {
    label: "Apply Now",
    // Always check if this route is correct.
    href: "/admissions/how-to-apply",
  },
} as const;
